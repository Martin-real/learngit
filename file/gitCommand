git command
git branch --set-upstream debug origin/debug 跟踪远程分支
git branch -vv 本地跟踪远程分支信息
git checkout --track origin/branch_name来在本地创建一个与branch_name同名分支跟踪远程分支。

==================================更新远程分支列表==============================================
git remote update origin --prune

git remote update origin -p